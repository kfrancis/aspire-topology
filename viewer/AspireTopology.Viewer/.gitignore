node_modules/
public/topology.isoflow.json

# dist/ is intentionally NOT ignored: AspireTopology.Hosting embeds it, so the .NET build never
# needs Node.js. Rebuild with `npm run build` and commit the result when the viewer changes.
dist/topology*.json
